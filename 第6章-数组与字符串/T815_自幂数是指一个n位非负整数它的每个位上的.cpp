/*
 * 来源：C++题库 题号815
 * 章节：第6章-数组与字符串
 * 难度：中
 * 题序：第89题
 *
 * 题目：自幂数是指一个 n 位非负整数，它的每个位上的数字的n次幂
 */

第89题 （0.1分）        题号:815        难度:中        第6章
/*--------------------------------------------------
【程序设计】
----------------------------------------------------
题目：自幂数是指一个 n 位非负整数，它的每个位上的数字的n次幂
      之和等于它本身。例如：个位数都是自幂数；153是自幂数，
      因为153 = 1*1*1 + 5*5*5 + 3*3*3。实现判断一个数是否为自
      幂数的函数self_power，用于找出0~10000000之间所有的自幂数。
      提示：计算一个数的n次幂可以使用math.h中定义的pow函数，
      例如，pow(x, n)表示x的n次幂。
---------------------------------------------------
注意：仅在标有"Program"和"End"的注释行之间补充填写代码。
      请勿改动主函数main和其它任何已有内容。
---------------------------------------------------*/


#include <iostream>
#include <fstream>
#include <math.h>
using namespace std;

bool self_power(int number)
{
/**********Program**********/
    
/**********  End  **********/
}

// [评分函数已省略]
    if (self_power(153))
        cout << "153是自幂数" << endl;
    else
        cout << "153不是自幂数" << endl;
    if (self_power(154))
        cout << "154是自幂数" << endl;
    else
        cout << "154不是自幂数" << endl;
    return 0;
}

#define N 10000000
void grading()
{
    fstream outfile;
    outfile.open("out.txt",ios::out);
    for (int i = 0; i < N; i++) {
        if (self_power(i)) {
            outfile<< i << endl;
        }
    }
    outfile.close();
}





/* 参考答案未提供 */

/*
 * 来源：C++题库 题号767
 * 章节：第6章-数组与字符串
 * 难度：中
 * 题序：第69题
 *
 * 题目：假定输入的字符串中只包含字母和*号
 */

第69题 （0.1分）        题号:767        难度:中        第6章
/*-------------------------------------------------------
【程序设计】
---------------------------------------------------------
题目：假定输入的字符串中只包含字母和*号。请编写函数fun，
      它的功能是：只删除字符串前导和尾部的*号，串中字
      母之间的*号都不删除。形参n给出了字符串的长度，形参
      h 给出了字符串中前导*号的个数，形参e给出了字符串中
      最后*号的个数。在编写函数时，不得使用C语言提供的字
      符串函数。
例如：字符串中的内容为：****A*BC*DEF*G*******，
      删除后，字符串中的内容应当是：A*BC*DEF*G。

注意：部分源程序在文件PROG1.C 文件中。
--------------------------------------------------
注意：仅在标有"Program"和"End"的注释行之间补充填
      写代码。请勿改动主函数main和其它任何已有内
      容。
-------------------------------------------------------*/

#include <stdio.h>
void NONO ();

void fun(char *a, int n,int h,int e)
{

/**********Program**********/






/**********  End  **********/

}
main()
{ 
        char s[81],*t,*f; 
        int m=0, tn=0, fn=0;
        printf("Enter a string:\n");
        gets(s);
        t=f=s;
        while(*t){t++;m++;}
        t--;
        while(*t=='*'){t--;tn++;}
        while(*f=='*'){f++;fn++;}
        fun(s, m, fn, tn);
        printf("The string after deleted:\n");
        puts(s);
        NONO();
}

void NONO ()
{/* 本函数用于打开文件，输入数据，调用函数，输出数据，关闭文件。 */
        FILE *in, *out ;
        int i ; char s[81], *t, *f ;
        int m=0, tn=0, fn=0;
        in = fopen("in.dat","r");
        out = fopen("out.dat","w");
        for(i = 0 ; i < 10 ; i++) {
                fscanf(in, "%s", s);
                t=f=s;
                m=0; tn=0; fn=0;
                while(*t){t++;m++;}
                t--;
                while(*t=='*'){t--;tn++;}
                while(*f=='*'){f++;fn++;}
                fun(s, m, fn, tn);
                fprintf(out, "%s\n", s);
        }
        fclose(in);
        fclose(out);
}


/* ───────── 参考答案 ─────────
char *p=a;
	int j=0,len=0;
	while(*p){p++; len++;}
	while(j<len-h-e)
	{
		a[j]=a[h+j];
		j++;
	}
	a[j]=0;
   ─────────────────────────── */
